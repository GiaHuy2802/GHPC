$(document).ready(function () {
    // Kích hoạt FancyBox cho các ảnh trong carousel
    $("#sync1 .item a").fancybox({
      width: 800,
      height: 600,
      // Các tùy chọn khác nếu cần
    });
  });